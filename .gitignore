# Banco SQLite gerado em tempo de execucao.
*.db

# Audios grandes do boletim (o smoke.wav continua versionado de proposito).
audio/boletim_*.wav

# Saidas geradas pelos testes (texto do boletim etc.).
output/

# HTML intermediario do gerador de PDF dos documentos de docs/ (o .md e o .pdf ficam versionados).
docs/*.html

# Modelos Kokoro (~337 MB, acima do limite de 100 MB por arquivo do GitHub).
# Reconstruir com: python scripts/download_kokoro.py
models/

# Artefato do Windows (redirecionamento para "nul").
nul

__pycache__/
*.py[cod]
.venv/
venv/
